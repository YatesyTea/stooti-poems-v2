{
  "scripts": {
    "dev": "bun run --hot src/index.tsx",
    "build": "bun build --target bun src/index.tsx --outdir ./out --watch"
  },
  "dependencies": {
    "hono": "^4.0.8"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
